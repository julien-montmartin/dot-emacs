dist: xenial

addons:
  apt:
    packages:
    - make
    - emacs

script:
  - make all

deploy:
  provider: pages
  skip_cleanup: true
  api_key: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: master
